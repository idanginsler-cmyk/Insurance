#!/usr/bin/env python3
"""
דמו 3: עיבוד מסמכים - סיכום, תרגום ושאלות
"""
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from demos.utils.config import call_llm, print_mode_banner, MOCK_MODE

SAMPLE_DOCUMENT = """
פוליסת ביטוח בריאות - סעיף כיסוי ניתוחים

5.2 כיסוי לניתוחים בישראל

5.2.1 המבטח ישפה את המבוטח בגין הוצאות ניתוח שבוצע בבית חולים
מוכר בישראל, ובלבד שהניתוח נדרש מבחינה רפואית ואושר מראש
על ידי הרופא המוסמך של המבטח, למעט במקרה של ניתוח חירום.

5.2.2 סכום הביטוח המרבי לניתוח: 300,000 ₪ לאירוע.

5.2.3 השתתפות עצמית: 1,500 ₪ לכל ניתוח.

5.2.4 בחירת מנתח: המבוטח רשאי לבחור מנתח מתוך רשימת המנתחים
המאושרים של המבטח. בחירת מנתח שאינו ברשימה תחייב אישור
מיוחד והשתתפות עצמית מוגדלת של 3,000 ₪.

5.2.5 חריגות לסעיף זה:
א. ניתוחים קוסמטיים שאינם נדרשים מבחינה רפואית
ב. ניתוחים לטיפול בהשמנת יתר (למעט BMI מעל 40)
ג. ניתוחי לייזר לתיקון ראייה
ד. ניתוחים הקשורים למצב רפואי קודם - ב-24 החודשים הראשונים

5.2.6 תקופת אכשרה: 6 חודשים לניתוחים אלקטיביים.
ניתוחי חירום - ללא תקופת אכשרה.
"""

MOCK_SUMMARY = """
📋 סיכום סעיף ניתוחים:

**מה מכוסה:**
- ניתוחים בבתי חולים מוכרים בישראל
- עד 300,000 ₪ לניתוח
- בחירת מנתח מרשימה מאושרת
- ניתוחי חירום - ללא תקופת אכשרה

**השתתפות עצמית:**
- 1,500 ₪ רגיל
- 3,000 ₪ אם בוחרים מנתח שלא ברשימה

**מה לא מכוסה:**
- ניתוחים קוסמטיים
- ניתוחי השמנה (למעט BMI מעל 40)
- לייזר לעיניים
- מצבים קודמים - 24 חודשים ראשונים

**תקופת אכשרה:** 6 חודשים (חירום - ללא)
"""

MOCK_SIMPLE = """
💬 בשפה פשוטה:

הביטוח מכסה ניתוחים בבתי חולים בישראל, עד 300,000 ₪.

אתם יכולים לבחור מנתח מהרשימה שלנו ולשלם 1,500 ₪ השתתפות עצמית.
אם אתם רוצים מנתח אחר - צריך אישור מיוחד ותשלמו 3,000 ₪.

חשוב: צריך לחכות 6 חודשים מתחילת הביטוח לפני ניתוח מתוכנן.
ניתוח חירום (למשל דלקת בתוספתן) - מכוסה מיד!

לא מכוסה: ניתוחים אסתטיים, ניתוח עיניים בלייזר, וניתוח
להורדת משקל (אלא אם ה-BMI שלכם מעל 40).
"""

MOCK_QA = """
❓ תשובות לשאלות:

ש: האם ניתוח להסרת שקדים מכוסה?
ת: כן, ניתוח להסרת שקדים מכוסה כניתוח רפואי נדרש.
   צריך אישור מראש ולחכות 6 חודשי אכשרה.
   השתתפות עצמית: 1,500 ₪.

ש: מה קורה אם אני צריך ניתוח דחוף?
ת: ניתוחי חירום מכוסים מיד, ללא תקופת אכשרה.
   עדיין יש השתתפות עצמית של 1,500 ₪.

ש: האם אני יכול לבחור כל מנתח שאני רוצה?
ת: אתה יכול לבחור מנתח מהרשימה המאושרת (השתתפות 1,500 ₪).
   אם רוצים מנתח אחר - צריך אישור + השתתפות 3,000 ₪.
"""


def main():
    print_mode_banner()

    try:
        from rich.console import Console
        from rich.panel import Panel
        console = Console()
        use_rich = True
    except ImportError:
        use_rich = False

    if use_rich:
        console.print(Panel("דמו 3: עיבוד מסמכים - סיכום, פישוט ושאלות", style="bold blue"))
    else:
        print("\n=== דמו 3: עיבוד מסמכים ===\n")

    print("📄 מסמך לדוגמה: סעיף כיסוי ניתוחים מפוליסת בריאות\n")

    # --- שלב 1: סיכום ---
    print("━" * 50)
    print("שלב 1: סיכום המסמך")
    print("━" * 50)

    if MOCK_MODE:
        print(MOCK_SUMMARY)
    else:
        result = call_llm(
            f"סכם את סעיף הביטוח הבא ב-4 קטגוריות: מה מכוסה, השתתפות עצמית, מה לא מכוסה, תקופת אכשרה.\n\n{SAMPLE_DOCUMENT}",
            "אתה מומחה ביטוח בריאות.",
        )
        print(result or MOCK_SUMMARY)

    # --- שלב 2: פישוט ---
    print("━" * 50)
    print("שלב 2: פישוט לשפה פשוטה")
    print("━" * 50)

    if MOCK_MODE:
        print(MOCK_SIMPLE)
    else:
        result = call_llm(
            f"הפוך את הסעיף הבא לשפה פשוטה שלקוח ללא רקע משפטי יבין. תן דוגמאות מעשיות.\n\n{SAMPLE_DOCUMENT}",
            "אתה כותב תוכן לצרכנים בשפה פשוטה וברורה.",
        )
        print(result or MOCK_SIMPLE)

    # --- שלב 3: שאלות ותשובות ---
    print("━" * 50)
    print("שלב 3: שאלות ותשובות על המסמך")
    print("━" * 50)

    if MOCK_MODE:
        print(MOCK_QA)
    else:
        questions = """
בהתבסס על הסעיף:
1. האם ניתוח להסרת שקדים מכוסה?
2. מה קורה אם אני צריך ניתוח דחוף?
3. האם אני יכול לבחור כל מנתח שאני רוצה?
"""
        result = call_llm(
            f"ענה על השאלות הבאות בהתבסס על סעיף הפוליסה:\n\n{SAMPLE_DOCUMENT}\n\nשאלות:{questions}",
            "אתה נציג שירות לקוחות בחברת ביטוח. ענה בצורה ברורה ומדויקת.",
        )
        print(result or MOCK_QA)


if __name__ == "__main__":
    main()
